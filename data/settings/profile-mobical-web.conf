name = mobical (web)
comment = This profile is suitable for the mobical web-service. You have to register and provide a username.
protocol = SyncML
mode = TwoWay

[Sources]
contacts.enabled = true
contacts.timestamp = 0
contacts.url = con
tasks.enabled = true
tasks.timstamp = 0
tasks.url = task
appointments.enabled = true
appointments.timestamp = 0
appointments.url = cal

[Transport]
type = Http
user = guest
password = guest
url = http://www.mobical.net/sync/server
